# Test/lint dependencies. Kept apart from requirements.txt so the runtime image
# does not ship a test client.
#
# Nothing here needs onnxruntime, kokoro-onnx, ALSA or a 310 MB model file: the
# suite fakes the kokoro session and patches the aplay subprocess, so it runs on
# a laptop. That is a deliberate property of this repo — see test/README notes
# in test/conftest.py.
-r requirements.txt
pytest>=7,<9
httpx           # required by fastapi.testclient.TestClient
ruff
